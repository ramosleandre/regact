# @package _global_
# No optional features (the default). The controller is always-on core - configured under
# `controller:` (conf/controller/*), not here. Select a real feature with e.g. `features=cwm`.
features: {}
