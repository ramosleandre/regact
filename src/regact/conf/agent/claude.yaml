# Claude Code CLI (subscription auth by default).
name: claude
model: opus            # e.g. "opus" / "sonnet" / "claude-opus-4-8"
args:
  permission_mode: bypassPermissions   # headless: no interactive prompts (deny-list still applies)
  effort: high                       # low | medium | high | xhigh | max
